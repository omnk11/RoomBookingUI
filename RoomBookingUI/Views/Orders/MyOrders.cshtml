@* @model IEnumerable<RoomBookingUI.Models.Order>

@{
    ViewData["Title"] = "My Orders";
}

<h2>My Booked Rooms</h2>

@if (Model.Any())
{
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Room Title</th>
                <th>City</th>
                <th>Price</th>
                <th>Booking Date</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var order in Model)
            {
                <tr>
                    <td>@order.RoomTitle</td>
                    <td>@order.City</td>
                    <td>@order.Price</td>
                    <td>@order.BookingDate.ToString("dd/MM/yyyy")</td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <p>You have not booked any rooms yet.</p>
}
 *@